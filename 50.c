#include <stdio.h>

void baklol(int n, int k)
{
    int i;
    int people[n];
    for (i = 0; i < n; i++)
    {
        scanf("%d", &people[i]);
    }
    for (i = 0; i<n; i++)
    {
        if (k -people[i]>=0)
        {
            printf("1");
             k = k - people[i];
        }
        else
        {
            printf("0");
           
        }
        
            // if (k >= people[i])
            // {
            //     printf("1");
            // }
            // else
                
    }
}
int main(void)
{
    int t;
    scanf("%d", &t);
    int n, k;
    // 	int people[n];
    scanf("%d", &n);
    scanf(" %d", &k);
    while (t)
    {
        baklol(n, k);
        t--;
    }

    return 0;
}
#include <stdio.h>

void baklol(int n, int k)
{
    int i;
    int people[n];
    for (i = 0; i < n; i++)
    {
        scanf("%d", &people[i]);
    }
    for (i = 0; i<n; i++)
    {
        if (k -people[i]>=0)
        {
            printf("1");
             k = k - people[i];
        }
        else
        {
            printf("0");
           
        }
        
            // if (k >= people[i])
            // {
            //     printf("1");
            // }
            // else
                
    }
}
int main(void)
{
    int t;
    scanf("%d", &t);
    int n, k;
    // 	int people[n];
    scanf("%d", &n);
    scanf(" %d", &k);
    while (t)
    {
        baklol(n, k);
        t--;
    }

    return 0;
}
#include <stdio.h>

void baklol(int n, int k)
{
    int i;
    int people[n];
    for (i = 0; i < n; i++)
    {
        scanf("%d", &people[i]);
    }
    for (i = 0; i<n; i++)
    {
        if (k -people[i]>=0)
        {
            printf("1");
             k = k - people[i];
        }
        else
        {
            printf("0");
           
        }
        
            // if (k >= people[i])
            // {
            //     printf("1");
            // }
            // else
                
    }
}
int main(void)
{
    int t;
    scanf("%d", &t);
    int n, k;
    // 	int people[n];
    scanf("%d", &n);
    scanf(" %d", &k);
    while (t)
    {
        baklol(n, k);
        t--;
    }

    return 0;
}
